<%- include('./partials/header') %>

    <h1> <%= title %> </h1>
    <p>Seleccioná tu heroe y te cuento que hace:</p>
    <h3><a href="/">Volver al Home</a></h3>
    <% for (var i=0; i < jasonParse.length; i++) { %>
        <%let detalleId = '/heroes/detalle/' + jasonParse[i].id %>
    <ul>
        <li><a href=<%= detalleId %>><%= jasonParse[i].nombre %></a></li>
    </ul>
    <% } %>

  </body>
</html>
